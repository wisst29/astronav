[project]
name = "astronav"
version = "1.0.0"
description = "Application de navigation astronomique maritime"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flet",
]

[tool.flet.app]
path = "main.py"

[tool.flet.flutter]
org = "com.astronav.navigation"
